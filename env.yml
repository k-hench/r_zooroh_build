# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: r_zooroh
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bioconda::plink=1.90b6.12
  - bioconda::vcftools=0.1.16
  - bioconda::bcftools=1.20